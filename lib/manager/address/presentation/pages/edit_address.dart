import 'package:flutter/material.dart';

import '../../../global/text_app.dart';

class EditAddressToPlaceOrder extends StatelessWidget {
  const EditAddressToPlaceOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: TextApp(text: "edit address"),
      ),
    );
  }
}
